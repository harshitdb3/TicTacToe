services:
  backend:
    build: .
    container_name: backend
    environment:
      - NODE_ENV=production
      - PORT=8080
      - REDIS_USERNAME=default  # Docker service name or host
      - REDIS_PASS=Q4JDH414H9w1hBhSHJfPXPTkClgSFFKT
    ports:
      - "8080:8080"
    expose:
      - "8080"
    networks:
      - tictactoe_net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080"]
      interval: 10s
      timeout: 5s
      retries: 3

networks:
  tictactoe_net:
    driver: bridge